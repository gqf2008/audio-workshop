<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  为什么需要 disable-library-validation：

  人声分离走 ort → ONNX Runtime，而本机（以及 homebrew 的封装）给的是**动态库**
  libonnxruntime.1.dylib。开了 hardened runtime 之后，macOS 会拒绝加载「不是本 Team ID
  签的」库 —— 实测启动直接死：

    dyld: Library not loaded: /opt/homebrew/opt/onnxruntime/lib/libonnxruntime.1.dylib
    Reason: mapping process and mapped file (non-platform) have different Team IDs

  （debug 版没签名所以一直没暴露；只有打包成 .app 才会撞上。）

  另一条路是把 libonnxruntime 连同它依赖的 ~90 个 homebrew dylib 一起塞进 .app 并逐个
  re-sign —— 复杂度高、升级 onnxruntime 就要重来一遍，暂不做。先用 Apple 给这个场景的
  正规 entitlement，并在文档里如实写明「需要本机有 onnxruntime」这个前提。
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
