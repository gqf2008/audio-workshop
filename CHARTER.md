# 音频作坊 · 立项说明书 v0.1

> 2026-09-15 立项。全部数据来自当日在本机（Apple M4 / 16GB / Metal）的实测，非估算。
> **2026-09-15 修订（与方案 v0.2 对齐）**：§2 第 3 条改为"人工投入 / 出片"两段并注明是目标非实测；§5 把断点续作与同 seed 复现移入开源、付费收敛为 8 项；§8 增"界面密度过高"风险；§9 的 M0/M1 范围收敛（M0 不追求界面、最小校听走命令行）。
> **2026-09-15 修订 2（第三轮独立评审后，与方案 v0.3 对齐）**：§2 第 3 条改为**条件式**（出片 ≤ 纯合成时间 ×1.2，"≤20 分钟"在 RTF≈4 端不可达）；§5/§9 **拆分模型下载器**（M1 基础下载免费 / P7 增强下载器 M4 付费）并校正项数口径（8 项 P1~P8）；§6/§8 引擎改为"**独立仓库 checkout（当前 v0.7.3），submodule 化待落**"（原文写 submodule 锁 tag，与仓库事实不符）；§7 更正 ace-step 备注（**按 RTF，不是"快 2.8×"**）；§10 统一判据分母（3 人）。

## 1. 定位

**本地优先的音频工作台**：自己的声音、自己的机器、自己的素材——不按字收费。

三场景统一设计（配音 / BGM / 歌曲），**配音先行**（唯一已被验证到可交付水平的场景）。

## 2. 成功标准（第一阶段：自己每天用）

| # | 可验证标准 |
|---|---|
| 1 | 连续 7 天，每天用它完成 ≥1 条真实内容生产 |
| 2 | 同类环节全程不退回剪映 / ElevenLabs |
| 3 | 单条 5 分钟口播稿：**人工投入 < 10 分钟**（含校听）· **出片 ≤ 纯合成时间 × 1.2**（wall clock，条件式：M0 端到端实测后定标；RTF≈4 端 ≤20 分钟不可达） |

基线（今日实测）：`audio8-tts` 可懂度 **98.5~100%**、RTF **2~4**（**单句短文本**）、峰值内存 2.3~4.1GB。

> 第 3 条是**目标，不是与 RTF 无关的硬阈值**：合成是串行关键路径，边合成边校听只压人工投入、**不缩短挂钟时间**（wall clock ≥ 合成总时长）。**2026-09-15 M0 定标**（audio8-tts / M4 16G / Metal）：1626 字 → 57 句 → 成品 **318.3s**；纯合成 **443.4s（RTF = 443.4 / 318.3 ≈ 1.39）**，拼装 0.21s，端到端 **443.7s = 纯合成 × 1.001**；ASR 回读可懂度 **99.86%**，0 句失败、0 次返工。人工投入按“全篇实时校听 318.3s + 操作估算 ≤60s”保守计 **≤6.3 分钟**（<10 分钟）；第 3 条当前成立。**RTF≈4 时 300s 音频纯合成就要 ≈20 分钟**，所以固定“≤20 分钟”在极端端**不可达**，仍统一按“**出片 ≤ 纯合成时间 ×1.2**”判定（方案 §2 步骤 4 / §5.3）。

## 3. 范围

**做**：配音 + BGM + 歌曲的统一工作台（三轨共享素材库与导出）
**不做**（第一阶段）：音色市场、云端推理、多人协作、克隆他人声音

## 4. 平台策略

Mac（M 系列 Metal）先行 → Windows（消费级 N 卡 CUDA）→ Linux。

工程含义：**Metal 性能必须自己扛**。上游对 Metal 的定位是"可移植性/测试"（服务启动即打印该警告），而创作者以 Mac 为主——这是第一阶段最大的技术投入，也是最能反哺上游的地方（已在 PR #554 上迈出第一步）。

## 5. 商业模式：开源 + 付费（边界设计）

**开源**（让模型变得可用的公共设施，回馈上游）：
- 配置层 `audio-config` / 文本兜底层 / 评估台 `audio-eval` / CLI 与服务集成
- 断点续作（跨会话续跑）与同 seed 复现 —— 已在 CLI 里成立：`cmd_synth` 跳过 `done`、重试 `error`，seed 是 `project.json` 的普通字段
- **基础模型下载器（M1，免费）**：从上游源拉取权重 + 手动指定模型路径

**付费**（产品本体，**8 项 P1~P8**，与方案 §4.2 一一对应；下面每条都带 P 编号，"一键安装"是桌面壳的交付形态、**不算第 9 个付费点**）：
- 桌面 App：一键安装（随壳交付，不单列）、自动更新与备份（P8）
- 批量与自动化：批量队列（P1）、模板与预设（P2）
- 素材与工程管理：音色库-多音色（P4）、发音词典库-多套（P5）、工程版本（P3）、分轨/批量导出（P6）
- 模型：**增强模型下载器（P7：队列 / 校验 / 断点续传 / 按机器推荐量化档）**
- 后续：云同步、协作

**下载器界线（第三轮评审裁决，与 §9 同步）**：**M1 = 基础模型下载（免费，随最小桌面壳交付）**；**P7 = 增强下载器（M4 付费）**。原文 §5 把"模型下载器"整条计入付费、§9 又把下载器列进 M1，两处打架，现按这条界线分开。

**许可红线**：只做下载器（M1 基础 + P7 增强），不打包权重。非商用（CC-BY-NC）模型不能随发行版分发——这是产品形态的硬约束，不是可选项。

## 6. 架构（今日已成型）

```
场景层   配音 / BGM / 歌曲        ← 创作者只看到这三个入口
配置层   models.schema.yaml       ← 模型旋钮 + known_issues + product_excluded
文本层   规范化 + 发音词典         ← 补上游缺口（如年份逐位读）
引擎层   audio.cpp（独立仓库 checkout，当前 v0.7.3）+ 服务/server
评估层   audio-eval               ← 可懂度/耗时/内存 + 回归对比
```

**引擎引入方式（第三轮评审更正）**：本仓库**既无 `.gitmodules` 也无 `external/`**（`external/` 是引擎仓库自己的目录）——引擎是**本机另一处 checkout**（当前锁在 **v0.7.3**），**submodule 化待落**；在那之前"锁版本"靠 checkout 上的 tag 记录。原文写的"以 submodule 锁定 tag 引入"与仓库事实不符。

**设计原则**：技术会进步、模型会换 —— 产品价值在配置层，不在改模型。上游模型不稳的地方（数字读法、0.1B 变体不可用）由配置层声明与兜底，不写死在代码里。

## 7. 实测基线（2026-09-15 · M4 16G · Metal）

| 模型 | 任务 | 可懂度 | 耗时 | 峰值内存 | 备注 |
|---|---|---|---|---|---|
| audio8-tts 0.6B | 配音 | **98.5~100%** | RTF 2~4 | 2.3~4.1G | **可交付** |
| audio8-tts-01b | 配音 | 0~3% | — | — | ❌ 不可用（时长乱跳）；PR #444 修的就是这条 0.1B 链路 |
| index-tts2 | 配音 | 96.3% | RTF 6.7 | 4.7G | 需 voice_ref；上游已接规范化 |
| stable-audio small | BGM | — | 30s 音频≈16s | — | 投入产出比最高 |
| yue2 q4_0 | 歌曲 | 中文 85% | 2'48"≈8 分钟（**RTF 2.86**） | — | 咬字好但慢 |
| ace-step turbo | 歌曲 | 中文 71% | 120s≈6.4 分钟（**RTF 3.2**） | — | 挂钟短只因曲子短（120s）：**按 RTF 比 yue2 更慢**（3.2 > 2.86）——不是"快 2.8×"（v0.1 把 RTF 当成了倍数） |
| qwen3-asr | 打分/转写 | — | ≈0.9s/14s 音频 | — | 评估台打分用 |
| sheetsage2 | 转谱 | — | — | — | 接 yue2 做翻唱 |

## 8. 风险与对策

| 风险 | 严重度 | 对策 |
|---|---|---|
| 剪映免费且够用 | 高 | 只打它给不了的：声音是自己的、素材不出机器、无限量 |
| Mac/Metal 性能需自扛 | 高 | 主动投入并反哺上游（#554 模式）；评估台量化每一步 |
| 模型许可（NC/不可分发） | 高 | 只做下载器；配置层区分"可商用/仅自用" |
| 上游节奏快（每日合并） | 中 | 引擎为**独立仓库 checkout（当前 v0.7.3），submodule 化待落**；滞后 1~2 版；适配只在自己薄层 |
| 歌曲质量不够产品级 | 中 | 降级为彩蛋，不作主打（8 分钟/首 + 质量不稳） |
| **界面密度过高** | **高** | 原型被真实用户打回（原话"看得我头皮发麻"：首屏 32 组件 / 句子行 7 元素 / 五区域全展开）→ 按方案 §3.1 红线收敛：首屏 ≤12 组件、句子行只留 序号+文本+状态点 |
| **未做用户验证** | **高** | 见第 10 节，与 M0 并行 |

## 9. 路线图

| 阶段 | 内容 | 完成判据 |
|---|---|---|
| **M0**（本周） | 配音链路做到"自己每天能用"（配置层 + CLI 收口 + 最小校听：顺序播放/逐句跳转，不追求界面） | 达成第 2 节三条标准（先跑一次真实 5 分钟稿量端到端） |
| M1 | 最小桌面壳（完整配音工作台：句子列表 + 校听器，波形/倍速/键盘翻句）+ **基础模型下载器（免费：上游源拉取 + 手动路径）** + 本地音色克隆（单条，免费） | 脱离命令行可自用 |
| M2 | BGM 接入（与配音同框，自动 ducking） | 一条视频的音频部分全用它 |
| M3 | Windows / Linux 支持（消费级 N 卡 CUDA 线） | 第二个平台可自用 |
| M4 | 歌曲场景 + 付费能力（批量、模板、工程管理 + **增强下载器 P7**） | 首个付费点成立 |

## 10. 用户验证（与 M0 并行，不阻塞）

首轮 **3 人**、最多扩到 5 人（**判据分母固定为首轮 3 人**，第 4~5 人只作补充样本）的自媒体创作者，用 M0 的产物做一件真事：**把一段 5 分钟口播稿用他自己的声音配出来**。
- 盲选对比：同一段稿子，剪映配音 vs 本产品输出
- 记录：愿意继续用吗？愿意付多少？
- **判据**：若 3 人中 ≥2 人愿意第二天继续用，M1 立项；否则先解决质量控制问题再投入界面

## 11. 待定

- 定价（订阅 / 买断 / 按量）
- 自研层用什么协议发布（倾向 Apache 2.0，与上游一致）
- 团队与投入节奏
